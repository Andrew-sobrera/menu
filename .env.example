VITE_API_URL=https://api.devchat.shop/api
VITE_APP_DOMAIN=devchat.shop
# Slug padrão quando o app roda em menu.devchat.shop (subdomínio reservado).
# Deve ser o slug real do restaurante na API (ex: menu, churrascaria-gaucha).
# Em wildcard (*.devchat.shop), pode ficar vazio.
VITE_DEFAULT_SLUG=menu
