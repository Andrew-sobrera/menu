import { MENU_HOST, RESTAURANT_SLUG } from '@/constants/app'

export function resolveRestaurantSlug(): string {
  return RESTAURANT_SLUG
}

export function resolveMenuHost(): string {
  return MENU_HOST
}
